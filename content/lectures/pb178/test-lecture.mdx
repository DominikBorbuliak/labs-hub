---
title: "Introduction to Security"
description: "A comprehensive overview of fundamental security concepts, threat models, and best practices for secure software development."
order: 1
recommendedStudyFrom: "2026-02-10"
recommendedStudyTo: "2026-02-16"
availableFrom: "2026-02-07"
tags:
  - security
  - introduction
  - fundamentals
---

# Comprehensive MDX Test Lecture

This is a paragraph with **bold text**, _italic text_, and **_bold italic text_**. You can also use <u>underscores</u> for emphasis.

## Heading Level 2

Here's a paragraph with some inline code: `const greeting = "Hello World"`. This is useful for referencing variables or short code snippets.

### Heading Level 3

Let's test some lists:

#### Unordered List

- First item
- Second item with a [link to GitHub](https://github.com)
- Third item
  - Nested item 1
  - Nested item 2
- Fourth item

#### Ordered List

1. First step
2. Second step
3. Third step with **bold text**
4. Fourth step

## Code Blocks

### JavaScript Example

```javascript
function calculateSum(a, b) {
  const result = a + b;
  console.log(`The sum is: ${result}`);
  return result;
}

const total = calculateSum(5, 10);
```

### Python Example

```python
def fibonacci(n):
    if n <= 1:
        return n
    else:
        return fibonacci(n-1) + fibonacci(n-2)

# Generate first 10 Fibonacci numbers
for i in range(10):
    print(f"F({i}) = {fibonacci(i)}")
```

### TypeScript Example

```typescript
interface User {
  id: number;
  name: string;
  email: string;
}

const users: User[] = [
  { id: 1, name: "Alice", email: "alice@example.com" },
  { id: 2, name: "Bob", email: "bob@example.com" },
];

function findUserById(id: number): User | undefined {
  return users.find((user) => user.id === id);
}
```

### C# / .NET Example

```csharp
using System;
using System.Collections.Generic;
using System.Linq;

public class User
{
    public int Id { get; set; }
    public string Name { get; set; }
    public string Email { get; set; }
}

public class UserService
{
    private List<User> users = new()
    {
        new User { Id = 1, Name = "Alice", Email = "alice@example.com" },
        new User { Id = 2, Name = "Bob", Email = "bob@example.com" }
    };

    public User? FindUserById(int id)
    {
        return users.FirstOrDefault(u => u.Id == id);
    }

    public async Task<List<User>> GetUsersAsync()
    {
        await Task.Delay(100); // Simulate async operation
        return users;
    }
}
```

### HTML/JSX Example

```jsx
export default function Button({ onClick, children }) {
  return (
    <button
      className="px-4 py-2 bg-blue-500 text-white rounded"
      onClick={onClick}
    >
      {children}
    </button>
  );
}
```

### Alert Boxes

> [!NOTE]  
> Highlights information that users should take into account, even when skimming.

> [!TIP]  
> Optional information to help a user be more successful.

> [!IMPORTANT]  
> Crucial information necessary for users to succeed.

> [!WARNING]  
> Critical content demanding immediate user attention due to potential risks.

> [!CAUTION]  
> Negative potential consequences of an action.

[^1]: This is the text of the note, [it can be a link too](https://www.example.com)

## Links and Emphasis

Here's a [link to MDX documentation](https://mdxjs.com/). You can also reference links like this: [GitHub][1] and [NPM][2].

[1]: https://github.com
[2]: https://npmjs.com

## Horizontal Rule

---

## Tables

Here's a table showing different programming languages and their use cases:

| Language   | Primary Use Case     | Type System | Year Created |
| ---------- | -------------------- | ----------- | ------------ |
| JavaScript | Web Development      | Dynamic     | 1995         |
| Python     | Data Science, AI/ML  | Dynamic     | 1991         |
| TypeScript | Large-scale Web Apps | Static      | 2012         |
| C#         | Enterprise, Game Dev | Static      | 2000         |
| Rust       | Systems Programming  | Static      | 2010         |

## Images

Below is an example of how images are rendered (if you add an actual image):

![Example Image](https://upload.wikimedia.org/wikipedia/commons/7/7d/Microsoft_.NET_logo.svg)

## Mixed Content

Here's a paragraph that combines multiple elements: you can use `inline code`, **bold text**, _italic text_, and [links](https://example.com) all together in one sentence.

### Task List (if supported)

- [x] Completed task
- [ ] Pending task
- [ ] Another pending task

## More Code Examples

### CSS

```css
.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
}

.button {
  background-color: #3490dc;
  color: white;
  padding: 10px 20px;
  border-radius: 4px;
  transition: background-color 0.3s ease;
}

.button:hover {
  background-color: #2779bd;
}
```

### JSON

```json
{
  "name": "my-project",
  "version": "1.0.0",
  "dependencies": {
    "react": "^18.2.0",
    "next": "^14.0.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build"
  }
}
```

### Shell/Bash

```bash
#!/bin/bash

# Install dependencies
npm install

# Run development server
npm run dev

# Build for production
npm run build
```

## Nested Content

This paragraph introduces a code block followed by an explanation:

```javascript
const data = await fetch("/api/users")
  .then((res) => res.json())
  .catch((err) => console.error(err));
```

The above code fetches user data from an API endpoint and handles any errors that occur.

## Final Notes

This document tests various Markdown and MDX features including:

1. **All six heading levels** (h1 through h6)
2. **Text formatting** (bold with `**text**`, italic with `*text*`, inline code)
3. **Lists** (ordered, unordered, nested)
4. **Code blocks** with syntax highlighting for multiple languages
5. **Blockquotes** for important callouts
6. **Links** and link references
7. **Horizontal rules** (`---`)
8. **Tables** with headers and data rows
9. **Images** with alt text

All of these should be properly styled! 🎨

---

### Additional Formatting Examples

Here's text with **strong emphasis** and _regular emphasis_. You can also combine them: **_both strong and emphasis_**.

> **Pro Tip:** Use blockquotes to highlight important information or tips for your readers. They stand out nicely with the blue accent!

---

_End of lecture_

```js
console.log("Hello world");
```

```csharp
public static void Main(string[] args)
{
    Console.WriteLine("Hello World!");
}
```
